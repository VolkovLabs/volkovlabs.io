---
tags:
  - Business Charts
  - Variables
image: /img/plugins/volkovlabs-echarts-panel/variables.png
---

import Code from "@theme/Code";
import Image from "@theme/Image";
import Video from "@theme/Video";

# Variables

Use the `replaceVariables()` function to replace dashboard and global variables.

```javascript
const email = replaceVariables("${__user.email}");
```

You can get familiar with three types of variables in our [Grafana Crash Course](/grafana/variables).

## Replace Variables

In the following example, we replaced the series's name with a value from the variable.

<Image
  title="Replace Dashboard Variables."
  src="/img/plugins/volkovlabs-echarts-panel/variables.png"
/>

### Example

<Code
  url="https://github.com/VolkovLabs/volkovlabs.io/blob/main/docs/volkovlabs-echarts-panel/js/replace.js"
  language="js"
/>

## Update Variables

You can update dashboard variables with [event handlers](/plugins/volkovlabs-echarts-panel/events), where

- `name` is the name of the variable. Add `var-` to update the variable value in the URL.
- `value` is the updated value.

```js
echartsInstance.on("click", (params) => {
  locationService.partial({ "var-name": value }, true);
});
```
