---
tags:
  - Apache ECharts
  - Events
image: /img/plugins/volkovlabs-echarts-panel/function.png
---

import Code from "@theme/Code";
import Image from "@theme/Image";
import Video from "@theme/Video";

# Extended Result

:::info Version

Apache ECharts Panel supports the extended result object starting from version 5.0.0.

:::

The extended result object allows you to return a configuration, options, and unsubscribe functions.

```json
return {
  version: 2,
  config: { notMerge: true },
  option: {
    tooltip: {
      formatter: '{a} <br/>{b} : {c}%'
    },
    series: [
      {
        name: 'Pressure',
        type: 'gauge',
        detail: {
          formatter: '{value}'
        },
        data: [
          {
            value: 50,
            name: 'SCORE'
          }
        ]
      }
    ]
  },
  unsubscribe: () => {
    console.log('unsubscribeFunction')
  }
};
```

## Version

The version is reserved for future improvements. The only supported version is 2.

## Configuration

| Option       | Description                              | Default |
| ------------ | ---------------------------------------- | ------- |
| `notMerge`   | Don't merge options.                     | `true`  |
| `lazyUpdate` | Useful when updating options frequently. | `false` |
| `silent`     | Whether trigger events.                  | `false` |

## Option

The option is the chart configuration in the JSON format described in [ECharts Functions](../options).

## Unsubscribe

The unsubscribe section allows the adding of JavaScript code which is executed before the object is destroyed.

One of the use cases relates to unsubscription from [Grafana events using Event Bus](../eventbus).
