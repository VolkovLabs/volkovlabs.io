---
tags:
  - Variable
image: /img/plugins/volkovlabs-variable-panel/favorites.png
---

import Image from "@theme/Image";

# Favorites

:::info Unique
Favorites are stored in the Web browser's storage and are unique for each user.
:::

The variable panel allows selecting values as favorites. The favorite icon in the header allows you to filter all selected favorites for quick navigation and selection.

<Image
  title="Selected values are unique for each user and stored in the Web browser's storage."
  src="/img/plugins/volkovlabs-variable-panel/favorites.png"
  lazy={false}
  width="80%"
/>
