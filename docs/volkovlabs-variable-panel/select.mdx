---
tags:
  - Business Variable
image: /img/plugins/volkovlabs-variable-panel/select.png
---

import Image from "@theme/Image";

# Minimized Mode

:::info Version
The minimized mode was introduced in version 1.6.0.
:::

The minimized mode is similar to the native implementation and works for Query and Custom variables.

Depending on the variable options user allow to select a single or multi-value in the multi-lines element.

<Image
  title="Multi-line select element allows to select and deselect multiple values."
  src="/img/plugins/volkovlabs-variable-panel/minimize.png"
/>

In the minimized mode the variable panel allows updating dashboard variables without occupying valuable top space and placing it anywhere on the dashboard.

<Image
  title="The Variable panel is an excellent alternative to the native implementation."
  src="/img/plugins/volkovlabs-variable-panel/select.png"
/>
